name: 'dataops'
version: '1.0.0'
config-version: 2

profile: 'dataops'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dataops:
    warehouse:
      +materialized: table
      +schema: warehouse
